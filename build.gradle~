plugins {
    id 'com.enonic.xp.app' version '1.0.17'
}

app {
    name = "${appName}"
    displayName = "${displayName}"
    vendorName = "${vendorName}"
    vendorUrl = "${vendorUrl}"
    systemVersion = "${xpVersion}"
}

dependencies {
    compile "com.enonic.xp:lib-guillotine:1.0.1"
    compile "com.enonic.xp:portal-api:${xpVersion}"
}

repositories {
    mavenLocal()
    jcenter()
    xp.enonicRepo()
}
